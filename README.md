# Freedom AI

실시간 협업 무한 캔버스 툴 — 브라우저만 있으면 바로 시작, 링크 공유로 팀과 함께 그립니다.

🔗 https://freedomai-app.vercel.app/

<img width="1276" height="553" alt="CleanShot 2026-09-16 at 09 00 03" src="https://github.com/user-attachments/assets/468c1752-6513-4502-8c65-ad7a469d4538" />



---

## 주요 기능

| 기능 | 설명 |
|------|------|
| 무한 캔버스 | 핀치 줌·패닝으로 끝없이 펼쳐지는 작업 공간 |
| 펜 & 지우개 | 3가지 굵기, 5가지 색상 / 소프트 브러시 지우개 |
| 포스트잇 | 추가·이동·리사이즈·텍스트 편집 |
| 이미지 삽입 | Ctrl+V 붙여넣기 또는 드래그 & 드롭으로 이미지 추가·이동·리사이즈 |
| 선택 툴 | 드래그로 여러 항목 범위 선택 후 한 번에 이동 |
| 실시간 협업 | Pusher 기반 커서·획·포스트잇 실시간 동기화 |
| Google 로그인 | Google 계정으로 즉시 입장, 로그아웃 시 계정 초기화 |
| 프로젝트 사이드바 | 좌측 사이드바에서 프로젝트 전환·추가·삭제 (`×` 버튼 + 확인 창) |
| 휴지통 | 삭제된 프로젝트 보관, 클릭으로 내용 열람, `×`로 영구 삭제 |
| 자유 이동 패널 | 상단 툴바·좌측 사이드바를 드래그로 자유롭게 이동 (PC), 위치 자동 저장 |
| 모바일 최적화 | 터치 드로잉·핀치 줌·이미지 터치 이동/리사이즈, 기종별 safe-area 대응 |
| 데이터 보존 | 프로젝트별 작업 내용 자동 저장 및 복원 (localStorage) |
| 공유 링크 | 버튼 한 번으로 URL 복사, 팀원 즉시 초대 |

---

## 기술 스택

| 분류 | 기술 |
|------|------|
| 배포 | Vercel (Serverless Functions) |
| 실시간 | Pusher Channels (Presence Channel + Client Events) |
| 저장소 | Vercel KV (Redis) + localStorage fallback |
| 인증 | Google Identity Services (GIS) OAuth 팝업 |
| 프론트엔드 | Vanilla JS, Canvas API |
| 백엔드 | Node.js Serverless (`/api/*.js`) |

---

## 프로젝트 구조

```
Freedom-AI/
├── api/
│   ├── action.js        # 캔버스 액션 처리 (획·지우개·포스트잇)
│   ├── ai-transform.js  # AI 이미지 변환 프록시 (HF_TOKEN 필요)
│   ├── remove-bg.js     # AI 배경 제거 프록시 (HF_TOKEN 필요)
│   ├── join.js          # 방 입장 & 초기 상태 반환
│   ├── projects.js      # 프로젝트 목록 동기화 (Google 토큰 검증)
│   ├── pusher-auth.js   # Pusher Presence 채널 인증
│   ├── room.js          # 방 상태 조회
│   └── stats.js         # 가입자/방문 통계 기록·조회
├── admin.html            # 통계 조회 페이지 (ADMIN_STATS_KEY 필요)
├── app.html             # 캔버스 앱 (메인)
├── index.html           # 랜딩 페이지
├── server.js            # 로컬 개발용 서버
├── vercel.json          # Vercel 배포 설정
└── package.json
```

---

## 로컬 개발

### 1. 설치

```bash
npm install
```

### 2. 환경변수 설정

`.env` 파일 생성:

```env
PUSHER_APP_ID=your_app_id
PUSHER_KEY=your_key
PUSHER_SECRET=your_secret
PUSHER_CLUSTER=your_cluster

# 선택 (없으면 localStorage로만 저장)
KV_REST_API_URL=your_kv_url
KV_REST_API_TOKEN=your_kv_token

# /admin.html 통계 페이지 접근 키
ADMIN_STATS_KEY=아무_임의_문자열
```

### 3. 서버 실행

```bash
npm start       # 일반 실행
npm run dev     # 파일 변경 감지 자동 재시작
```

### 4. 테스트

```bash
npm test
```

`api/*.js`의 핵심 동작(획/노트/이미지/도형 삭제·이동, 프로젝트 이름 변경, 방 입장)을
실제 KV·Pusher 없이 인메모리 가짜 구현으로 검증한다 (`tests/`, Node 내장 테스트
러너 사용, 별도 설치 불필요 — `@vercel/kv`·`pusher`는 `npm install`로 이미 받는
의존성을 그대로 재사용). 소유권 정책처럼 한 번 조용히 깨졌던 적 있는 동작은
회귀 테스트로 고정해뒀다.

---

## Vercel 배포

### 필수 환경변수

| 변수명 | 설명 |
|--------|------|
| `PUSHER_APP_ID` | Pusher 앱 ID |
| `PUSHER_KEY` | Pusher 키 |
| `PUSHER_SECRET` | Pusher 시크릿 |
| `PUSHER_CLUSTER` | Pusher 클러스터 (예: `ap3`) |
| `KV_REST_API_URL` | Vercel KV URL (선택) |
| `KV_REST_API_TOKEN` | Vercel KV 토큰 (선택) |
| `ADMIN_STATS_KEY` | `/admin.html` 통계 페이지 접근 키 |

### Pusher 설정

- Pusher 대시보드 **App Settings → Enable client events** 활성화 필수

### Google OAuth 설정

Google Cloud Console → 사용자 인증 정보 → **Authorized JavaScript origins** 에 배포 URL 추가:

```
https://your-domain.vercel.app
```

---

## 단축키 (PC)

| 키 | 기능 |
|----|------|
| `P` | 펜 도구 |
| `E` | 지우개 |
| `S` | 포스트잇 |
| `H` | 이동(핸드) 도구 |
| `V` | 선택 툴 |
| `Ctrl + Z` | 실행 취소 |
| `+` / `-` | 줌 인/아웃 |
| `0` | 화면 초기화 |

---

## 모바일 사용법

| 동작 | 기능 |
|------|------|
| 한 손가락 드래그 | 현재 툴 동작 (그리기·이동 등) |
| 두 손가락 핀치 | 줌 인/아웃 |
| ☰ 버튼 | 프로젝트 사이드바 열기/닫기 |
| 사이드바 외부 터치 | 사이드바 닫기 |
| 이미지 드래그 | 이미지 이동 |
| 이미지 우하단 핸들 드래그 | 이미지 리사이즈 |

---

## 주요 정책

- 룸당 최대 스트로크 1,000개
- 포스트잇 텍스트 최대 10,000자 / 크기 100px ~ 3,000px
- 이미지: 붙여넣기·드롭 시 최대 1,200px로 자동 압축 (JPEG 82%)
- 지우개: 클라이언트에서 직접 계산 후 결과 전송 (KV 없이도 동작)
- 삭제·이동(포스트잇/이미지/도형/획): 소유자 구분 없이 방의 누구나 가능 (지우개와 동일 정책)
- 데이터: Vercel KV 미설정 시 localStorage에 프로젝트별 자동 저장
- 패널 위치(툴바·사이드바): PC에서 드래그 이동, localStorage에 저장

---

## 로드맵

### 완료 ✅

- 무한 캔버스, 펜·소프트 지우개, 포스트잇
- 이미지 삽입 (붙여넣기·드래그 드롭·이동·리사이즈·삭제)
- 선택 툴 (범위 드래그 → 다중 항목 일괄 이동)
- Pusher 실시간 협업 (커서·획·포스트잇 동기화)
- Google 로그인 / 로그아웃 (매번 계정 선택 강제)
- 프로젝트 사이드바 (전환·추가·삭제·휴지통·데이터 보존)
- 상단 툴바·좌측 사이드바 자유 이동 (PC, 위치 저장)
- 모바일 터치 최적화 (핀치 줌, 터치 드로잉, 이미지 터치, safe-area 대응)
- Vercel 서버리스 배포
- 도형 도구 (사각형, 원, 삼각형, 화살표)
- AI 이미지 변환 (프롬프트 스타일 변환) / AI 배경 제거

### 예정 🔜

- AI 아이디어 제안

---

## 변경 이력

변경할 때마다 이 목록에 새 날짜로 추가합니다 (최신순). 2026-09-13 이전 이력은
`git log`로 확인할 수 있습니다.

### 2026-09-20
- 관리자 통계(`/api/stats`) 페이지뷰 기록 시 KV 쓰기 에러를 조용히 삼키던 것을 수정 — pageviews 카운터와 일별/월별 카운터를 각각 별도 try/catch로 분리하고 실패 시 Vercel 함수 로그에 남기도록 함. 오늘 날짜(9/20) 방문 카운팅이 안 되는 것 같다는 제보로 점검하던 중 발견 — 겉에서는 항상 204(성공)로 응답해서 실제로 어느 카운터가 실패하는지 알 수 없던 상태였음
- 전체 점검(병렬 에이전트 4개)으로 발견한 버그 10개 수정:
  - `todo_toggle`/`todo_delete`에 날짜 형식 검증이 빠져있어 `date:"__proto__"` 같은 입력으로 서버가 500 크래시 나던 문제 수정
  - 날짜당 할 일 50개 한도 초과 시 서버가 조용히 거부하면서도 항상 성공 응답을 보내, 클라이언트가 이미 화면에 추가해버린 항목이 동기화 안 된 채 새로고침하면 사라지던 문제 수정 — 서버가 `rejected` 플래그를 보내고 클라이언트가 낙관적 추가를 되돌리도록 함
  - todos 기능 이전에 만들어진 방(레거시 방)이 `todos` 필드 없이 내려와 프론트가 깨질 수 있던 문제를 `api/join.js`·`api/room.js` 양쪽에서 수정
  - 포스트잇 자석 스냅 거리가 확대/축소(줌) 배율을 반영 안 해서 많이 확대하면 너무 쉽게 붙고 축소하면 거의 안 붙던 문제 수정
  - 게스트가 모바일에서 햄버거(☰) 버튼을 눌러도 아무 반응 없던 죽은 버튼 문제 수정 — 게스트는 사이드바가 없으므로 버튼 자체를 숨김
  - 랜딩 페이지 "소개" 메뉴가 존재하지 않는 `#about` 앵커를 가리키던 죽은 링크 수정 — 푸터로 연결되도록 함
  - "색상 팔레트" 카드가 실제로 없는 "HEX 직접 입력" 기능을 광고하던 문구 정정
  - 히어로 목업에 남아있던 장식용 포스트잇 3개(보라·민트·로즈) 정리 — "포스트잇 5가지 색상" 카드 문구와 모순되던 것을 해소하고, 그걸 위해 넓혔던 히어로 폭도 원래대로 되돌림
  - "협업 To-Do"가 기능 카드에서는 Phase 1(완성)인데 로드맵 섹션에서는 여전히 Phase 2(예정)로 남아 자기모순이던 것을 로드맵도 Phase 1로 옮겨 정리
  - 관리자 통계 페이지가 재조회 실패 시 이전 조회 결과(그래프·목록)를 그대로 남겨둬서 최신 데이터로 오해할 수 있던 것을 수정 — 실패 시 전부 숨기도록 함
  - 관련 회귀 테스트 4개 추가 (날짜 검증, 한도 초과 응답, 레거시 방 todos 백필 2건)
- 위 수정 사항들을 재점검(병렬 에이전트 3개)하는 과정에서 추가로 발견한 문제 수정 — 관리자 페이지 연락처(문의) 목록도 통계 그래프와 같은 패턴으로, 재조회 실패 시 이전 문의 목록이 그대로 남아있던 것을 수정

### 2026-09-19
- 랜딩 페이지 "협업 To-Do" 카드 문구를 실제 기능(캘린더에서 날짜 선택 후 할 일 작성)에 맞게 정정하고 Phase 태그를 "Phase 1 · 핵심"으로 변경 — 이전엔 "캔버스 안에" 만든다고 돼있어 실제 구현(캘린더 모달)과 달랐음

### 2026-09-18
- 랜딩 페이지 "AI 이미지 편집" 카드 문구를 "AI 배경 제거"로 변경 — AI 프롬프트 변환·생성 기능은 Hugging Face 무료 프로바이더(hf-inference)가 GPU 이미지 생성을 더 이상 지원하지 않아 현재 작동하지 않는 상태라, 실제로 되는 배경 제거 기능만 안내하도록 문구를 정정함
- 랜딩 페이지 "포스트잇" 카드 문구를 실제 기능(색상 5개, 크기 조절, 화살표 연결, 자석 스냅)을 반영하도록 다시 작성
- 툴바에 "템플릿" 버튼 추가 — 클릭하면 뜨는 팝오버에서 칸반보드(할 일·진행 중·완료 3열, 각 열에 안내용 포스트잇 2장)를 클릭 한 번으로 캔버스에 삽입. 기존 있던 실체 없는 "협업 To-Do" 카드를 실제로 동작하는 "보드 템플릿" 기능으로 대체하고 랜딩 페이지 카드 문구도 정정
- 위 보드 템플릿 기능을 되돌림
- 툴바에 캘린더 아이콘 추가 — 클릭하면 월간 캘린더 모달이 뜨고, 이전/다음 달 이동과 날짜 선택 가능
- 캘린더에서 날짜를 클릭하면 그 날짜의 To-Do 목록을 작성·체크·삭제할 수 있게 함 — 팀원들과 실시간으로 동기화되고 새로고침해도 유지됨(room state에 저장). 할 일이 있는 날짜는 캘린더에 작은 점으로 표시

### 2026-09-17
- 로그인 모달에 "게스트로 입장" 버튼 추가 — Google 로그인 없이도 캔버스를 바로 체험할 수 있게 함 (안내 문구로 작업 내용이 저장되지 않음을 고지)
- 랜딩 페이지 헤더의 "로그인" 버튼이 별도 Google 팝업을 바로 띄우던 것을 수정 — app.html로 이동해 게스트 입장 옵션이 있는 로그인 모달이 먼저 뜨도록 함
- 게스트로 입장 시 이 브라우저에 남아있던 (이전 로그인 계정의) 최근 프로젝트 목록·사이드바가 그대로 노출되던 문제 수정 — 게스트는 프로젝트 목록·사이드바를 아예 볼 수 없도록 함
- 게스트로 입장 시 "로그아웃" 버튼이 노출되던 문제 수정 — 로그인한 적이 없으므로 게스트에게는 표시하지 않음
- 게스트가 입력한 프로젝트 이름이 이 브라우저에 저장되어, 나중에 같은 브라우저로 실제 로그인하면 "최근 프로젝트"에 섞여 나오던 문제 수정 — 게스트 세션은 로컬 기록을 아예 남기지 않음
- 게스트가 새로 만든 방(캔버스 데이터)이 서버에 영구히 남던 것을 수정 — 마지막 활동 24시간 후 자동 삭제되도록 함 (기존에 있던 진짜 프로젝트와 이름이 겹쳐도 만료가 걸리지 않도록 안전장치 포함)
- 게스트 입장 확인 문구에서 "전부" 표현 삭제
- 작은 크기로 빠르게 드래그하면 지우개가 획을 못 지우는 문제 수정 — 지우개 경로를 점이 아니라 선분으로 판정하도록 해서, 드래그가 빨라 마우스 이동 샘플 간격이 벌어져도 지나간 자리는 확실히 지워지게 함
- 관리자 통계 페이지(`/admin.html`)에 일별 방문 수 막대그래프 추가 (최근 30일, KST 기준 날짜별 집계)
- 관리자 통계 페이지에 월별 방문 수 막대그래프도 추가 (최근 12개월, KST 기준 월별 집계)
- 로고에서 F 글자를 감싸던 흰색 둥근 사각형 테두리 제거 — F가 검은 배경에 바로 놓이도록 단순화
- 로고를 F 글자만 남기고 둥근 사각형 배경(폴더 모양) 전체를 제거 — F 모양은 그대로 두고 투명 배경 위 검은 F만 표시
- 위 변경들을 모두 되돌리고 로고를 원래 디자인(흰색 둥근 사각형 테두리 + 검은 F)으로 최종 복원
- 상단바 "Freedom AI" 글씨를 굵게 변경 (font-weight 500 → 700)
- 랜딩 페이지 헤더 "Freedom AI" 글씨도 굵게 변경 (누락됐던 부분 추가)
- 로고에서 바깥 검은 폴더 모양(둥근 사각형 + 접힌 모서리)만 투명 처리해 제거 — 안쪽 흰색 둥근 사각형과 F는 그대로 유지
- 위 변경을 되돌리고 로고를 원래 디자인(검은 폴더 배경 + 흰 테두리 + 검은 F)으로 복원
- "Freedom AI" 로고 글씨체를 KOTRA 볼드체로 변경 (상단바·랜딩 페이지 헤더)
- 로고 옆 "Freedom AI" 글씨를 "FREEDOM AI" 대문자로 변경 (상단바·랜딩 페이지 헤더·푸터)
- 위 대문자 표기를 "freedom ai" 소문자로 변경
- 표기를 "Freedom Ai"로 최종 변경
- 랜딩 페이지 헤더/푸터 로고 아이콘을 감싸던 흰 배경·검은 테두리 박스 제거 — 아이콘이 바로 보이도록 단순화
- 랜딩 페이지 히어로 캔버스 목업의 빈 공간에 포스트잇 2개 추가 (링크 공유, 모바일 지원)
- 위 변경을 되돌림 — 포스트잇 2개 제거
- 히어로 문단(.hero-desc) 최대 폭을 560px에서 780px로 넓혀 캔버스 목업이 실제로 780px까지 커지도록 수정하고, 오른쪽에 생긴 공간에 포스트잇 3개(링크 공유, 모바일, 구글 로그인) 추가
- "구글 로그인" 포스트잇 위치를 오른쪽 끝에서 왼쪽 빈 공간으로 이동
- 이미지에서 글씨(로고·타이포그래피)만 추출하는 배경제거가 흰 배경만 지원하던 것을 검은 배경도 지원하도록 확장 — 이미지 가장자리 밝기로 배경이 밝은지 어두운지 먼저 판별해서 방향을 자동으로 맞춤
- 검은 배경 판별이 모서리 몇 점만 보다가, 글씨가 가장자리까지 꽉 찬 타이포그래피(예: "FONT" 같은 큰 글자가 모서리를 침범하는 경우)에서 틀리던 문제 수정 — 이미지 전체의 어두운/밝은 픽셀 개수를 세어 다수결로 판별하도록 변경
- 포스트잇을 드래그해서 다른 포스트잇 가장자리 가까이 가져가면 자석처럼 딱 붙는 스냅 기능 추가 — 같은 의견을 보완하는 포스트잇끼리 깔끔하게 이어붙일 수 있음

### 2026-09-16
- 이미지에 배경 제거 전용 버튼 추가 (호버 시 좌상단에 가위 아이콘, 꾹 누르기 없이도 바로 실행 가능)
- 로고·텍스트 이미지에 배경제거를 쓰면 AI가 글자를 엉뚱하게 잘라내던 문제 수정 — 명도 분포로 사진인지 그래픽인지 미리 판별해서, 그래픽이면 AI 없이 밝은 배경만 제거
- 처리 중 토스트의 깨진 이모지 아이콘을 점선 원 SVG로 교체
- README에 날짜별 변경 이력(changelog) 섹션 추가
- 선택 툴에서 이미지를 클릭만 해도 선택되도록 수정 (이전엔 범위 드래그로만 선택되어, 클릭 한 번으로는 Backspace/Delete 삭제가 안 됐음)
- 이미지 우클릭으로 "맨 앞으로 가져오기"/"맨 뒤로 보내기" 추가 — 배경 제거한 이미지끼리 겹칠 때 순서 조절 가능, 새로고침 후에도 순서 유지
- 관리자 통계 페이지(`/admin.html`)에 가입자 수뿐 아니라 실제 가입 이메일 목록도 표시

### 2026-09-15
- 배경제거 처리 중에만 실루엣 주변에 점선 컷라인을 보여주고, 완료되면 사라지도록 함
- AI 배경제거·이미지변환 결과가 마음에 안 들면 Ctrl+Z로 원본으로 되돌릴 수 있게 함
- 배경제거 후 이미지 주변에 남아있던 사각형 그림자 제거
- 랜딩 페이지를 Neo-Brutalism 스타일로 리디자인
- 코드 전체 점검(프론트엔드/백엔드/신규 서버로 나눠 병렬 점검) 후 발견된 크래시·보안·동기화 버그 수정
- 배경제거·AI이미지변환 요청을 별도 서버(Render)로 분리해, Vercel 계정 문제와 무관하게 항상 동작하도록 함

### 2026-09-14
- 랜딩 페이지 히어로 문단의 한글 줄바꿈(단어 중간 끊김) 버그 수정
- 복사해서 붙여넣은 글자가 폰트 크기 조절 안 되던 문제 수정
- 포스트잇 핸들 확대 + 상단 툴바에서도 폰트 크기 조절 가능하게 개선
- 포스트잇 글자 크기 조절 기능 최초 추가
- 스페이스바로 패닝 중 다른 요소가 대신 움직이거나 손 커서가 안 보이던 문제 수정
- 배경 제거를 모달 버튼 대신 이미지 꾹 누르기(long-press)로 단순화, 관련 호출 실패/시간초과 버그 수정
- AI 배경 제거 기능 최초 추가
- 핵심 백엔드 동작(획·포스트잇·이미지·도형 삭제/이동, 방 입장)에 자동 테스트 추가
- 삭제·이동이 서버에 반영되지 않고 재접속하면 되살아나던 문제 근본 수정 (userId 고정 + 소유권 정책 정리)
- 사이드바 프로젝트 우클릭 메뉴에 이름 변경·저장 기능 추가
- 툴바에 실행취소(Undo) 버튼 추가

---

© 2025 Freedom AI
